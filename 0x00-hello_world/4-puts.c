#include <stdio.h>

/**
 * main - Entry point
 * puts(Programming is like building a multilingual puzzle with a newline
 * Return: Always 0 (Success)
 */

int main(void)
{
puts("\"Programming is like building a multilingual puzzle\n");
return (0);
}
